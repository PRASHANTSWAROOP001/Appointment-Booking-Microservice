import { Request, Response } from "express";
import { PrismaClient, Prisma } from "@prisma/client";
import logger from "../utils/logger";

const searchAllServices = async(req:Request, res:Response):Promise<void>=>{
    try {
        
    } catch (error) {
        
    }
}